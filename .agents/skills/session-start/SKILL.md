---
name: session-start
description: セッション開始時（/clear 後や作業再開時）に、確定設定と進捗を素早く復元する。MEMORY.md・HISTORY.md を読み込み、進捗サマリーを提示し、作業目標に応じて使うべきスキル・エージェントを案内する。Triggers include "session start", "作業開始", "再開", "続きから", "何やってたっけ".
---

# session-start

セッションの入口。散らばった記憶を机の上に一度並べ直すイメージで、今どこにいるかを短時間で復元する。

## 手順

1. **正典と履歴を読む**
   - `.claude/MEMORY.md`（確定設定の正典＋タスクトラッカー）
   - `.claude/HISTORY.md`（直近の変更ログ・章サマリー）
   - 必要なら `AGENTS.md`（リポジトリ直下・原則・視点の再確認）

2. **並行チャットの書き置きを確認する**（プロトコル → `.claude/comm/README.md`）
   - チャット名（`chat-<レーン名>`）を確認する。担当レーンが不明ならユーザーに確認する。
   - main チェックアウト（`dev/fantasy_novel/.claude/comm/`）の outbox から自分宛（`@chat-<自分>`・`@all`）の最新エントリを確認する。worktree 内のコピーは読まない。
   - `comm/decisions/ANSWERS.md` に自分の D-ID への回答が来ていれば、該当エントリを自分の decisions ファイルから消して着手候補に載せる。

3. **進捗サマリーを提示する**（結論先出し・簡潔に）
   - 今の作品状況（執筆状況・総字数）。
   - タスクトラッカーの「進行中／直近の完了／予定」。
   - 他チャットからの自分宛の書き置き・回答があれば要点を添える。
   - 未決の【未定】のうち、次に効くもの（特に最優先＝「才能の仕組み」など）。

4. **作業目標に応じて案内する**
   - 構想・章設計をやる → `novel-author` エージェント。
   - 本文を書く → `novel-writer` エージェント（`rules/writing.md`・`prose-craft.md` に従う）。
   - 書いた文章を磨く → `novel-prose-coach`。
   - 展開に迷い・視野が固まった → `novel-devil` で代替案。
   - 原稿を点検する → `/manuscript-review`。
   - 設定を確定・追加する → 提案して MEMORY.md（正典）に状態タグ付きで反映。

## 原則

- 設定は正典（MEMORY.md・設計/）を正とする。recall や記憶が食い違ったら正典を優先する。
- 【未定】の設定で本文を書き進めない。決めるべきことは先に作者に確認する。
- 提案は前のめりに。ただし正典・設計の変更は作者の承認を挟む。
